/**
 * @typedef {import('express').Request} Request
 * @typedef {import('@prisma/client').User} User
 * 
 * @typedef {Request & {user?: User}} AuthenticatedRequest
 */

// This file contains JSDoc type definitions for JavaScript
// The types are now defined using JSDoc comments instead of TypeScript interfaces 